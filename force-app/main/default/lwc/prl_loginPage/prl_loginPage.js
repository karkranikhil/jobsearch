import { LightningElement, track } from 'lwc';
//import globalCss from "@salesforce/resourceUrl/psl_general";
//import prlImage from "@salesforce/resourceUrl/pslLoginImages";


export default class LoginPage extends LightningElement {
  //  @track loginBG = prlImage + '/loginbg.png';
    // connectedCallback() {
    //     Promise.all([loadStyle(this, globalCss + "/psl_general.scss")]);
    // }
}